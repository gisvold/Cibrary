@model IEnumerable<Cibrary.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryId })
            @Html.ActionLink("Details", "Details", new { id=item.CategoryId }) 
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryId })
        </td>
    </tr>
}

</table>


@using (Html.BeginForm("Index", "Category", FormMethod.Post))
    {
           
            @Html.TextBox("name") 
            <i class="icon-plus"></i>
            <input type="submit" class="submitcategori" value="legg til ny kategori" />
           
    }
<p> <!-- Html.ActionLink("Legg til kategori", "Create")--></p> 





